---
# Dependency manifest for `ansible-galaxy collection install -r
# requirements.yml` - pins the AWS module collections the roles need.
collections:
  - name: amazon.aws
    version: ">=10.0.0"

  - name: community.aws
    version: ">=10.0.0"
